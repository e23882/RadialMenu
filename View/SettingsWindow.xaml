<Window
    x:Class="RadialMenu.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    Title="Settings"
    Width="400"
    Height="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            HorizontalAlignment="Left"
            Orientation="Horizontal">
            <CheckBox VerticalAlignment="Center" IsChecked="{Binding MouseMenuEnable}">
                啟用滑鼠選單
            </CheckBox>
            <TextBlock
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Text="啟用按鍵" />
            <ComboBox
                Margin="10,0,0,0"
                ItemsSource="{Binding AllKeyCollection}"
                SelectedItem="{Binding SelectedKey}" />
        </StackPanel>
        <Button
            Grid.Row="4"
            Grid.Column="1"
            Margin="3"
            Padding="5"
            Background="Transparent"
            Content="SAVE" />
        <Button
            Grid.Row="4"
            Grid.Column="2"
            Margin="3"
            Padding="5"
            Background="Transparent"
            Content="CANCEL" />
    </Grid>
</Window>
